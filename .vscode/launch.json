{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Iniciar servidor Django",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/manage.py",
            "args": ["runserver"],
            "django": true
        }
    ]
}
